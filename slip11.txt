# Define lists
list1 <- c("x", "y", "z")
list2 <- c("X", "Y", "Z", "x", "y", "z")

# Find elements in list2 that are not in list1
result <- setdiff(list2, list1)

# Display result
print(result)






import pandas as pd
import matplotlib.pyplot as plt
from sklearn.cluster import AgglomerativeClustering
from scipy.cluster.hierarchy import dendrogram, linkage

# Load dataset (replace with your local path or GitHub raw link)
data = pd.read_csv("Wholesale_customers.csv")

# Select numerical columns for clustering
X = data.select_dtypes(include=['float64', 'int64'])

# Create dendrogram
linked = linkage(X, method='ward')
plt.figure(figsize=(10, 7))
dendrogram(linked)
plt.title("Wholesale Customers Dendrogram")
plt.xlabel("Customers")
plt.ylabel("Euclidean Distances")
plt.show()

# Apply Agglomerative Clustering
cluster = AgglomerativeClustering(n_clusters=3, affinity='euclidean', linkage='ward')
y_clusters = cluster.fit_predict(X)

# Add cluster labels to dataset
data['Cluster'] = y_clusters
print(data.head())
